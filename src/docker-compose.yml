#docker-compose.yml (Base)
version: '3.4'
services:
  cocktailmaker:
    image: cocktailmaker:${TAG:-latest}
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    build:
      context: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
